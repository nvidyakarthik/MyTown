<div class="row">

    <div class="col-2">
        <h4 class="subtitle">Filter By:</h4>
        <div id="filters">
            
            <ul>
                {{#each categoryList}}
                <li>
                    <a href="/api/posts/{{id}}">{{categoryName}}</a>

                </li>
                {{/each}}
            </ul>

           
            
        </div>
    </div>


    <div class="col-6">
        <h3 class="subtitle">Things to Always Do</h3>
        <div id="activities">
            <ul>
                {{#each activities}}
                <li>
                    <a href="#">{{name}}</a>

                </li>
                {{/each}}
            </ul>
        </div>
    </div>

    <div class="col-4">
        <h3 class="subtitle">Happening this Weekend</h3>
        <div id="weekend">
        </div>
    </div>
</div>
</div>